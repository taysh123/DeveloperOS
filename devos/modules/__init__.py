"""Product modules, filled in per roadmap phase.

Planned (see docs/ROADMAP.md): brain, codemap, debug, tasks, memory, search,
docgen, action (safe action agent), git, automation. Each will expose a small
service API consumed by the CLI and, later, the dashboard.
"""
