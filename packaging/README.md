# Packaging DeveloperOS (desktop ladder step C — D-0031)

Builds a single-file **`DeveloperOS.exe`** that wraps the `devos app` launcher (D-0030):
double-click → the local dashboard starts (or is reused) → the browser opens. Local-first
and offline exactly like the pip install; the exe is the same code, packaged.

## Repeatable build

```powershell
cd packaging
./build.ps1          # installs PyInstaller if missing, then builds
# -> packaging/dist/DeveloperOS.exe
```

Manual equivalent: `pip install pyinstaller` then `pyinstaller --noconfirm --clean devos.spec`.

**PyInstaller is a dev-time dependency only** — the DeveloperOS runtime stays stdlib-only
(D-0005). Nothing in `pyproject.toml` changes.

## What the bundle contains

- The `devos` package (entry: `launch_devos.py` → `devos.cli.main(["app", ...])`;
  extra exe arguments pass through, e.g. `DeveloperOS.exe --port 8770 --no-browser`).
- Data files: `devos/api/static/**` (vendored dashboard, PWA manifest, icons) and
  `devos/storage/schema.sql` (read via `importlib.resources`).
- `devos.ico` — multi-size (16/32/48/256) Windows icon of the D-0029 brand mark,
  regenerated by `python tools/make_icons.py` (stdlib-only ICO/PNG writers).

## Choices (recorded in D-0031)

- **onefile** — single artifact for non-technical users; slower first start (self-extract)
  is acceptable for a local tool.
- **console=True** for now — the console window is the server log and the Ctrl+C handle;
  a windowless/tray variant is a later refinement.
- **upx=False** — packed executables trigger antivirus false positives.
- **No CI builds** — the exe is built on demand by a developer; a release workflow is a
  candidate for ladder step D (installer + GitHub Releases distribution).

## Versioning

The exe reports the package version (`DeveloperOS.exe` serves the dashboard whose System
status shows `devos.__version__`); bump versions exactly as for pip releases. Embedding a
Windows VERSIONINFO resource is a step-D refinement.

## Remaining ladder steps (FUTURE_ROADMAP §8)

- **D:** Inno Setup installer (Start-Menu shortcut, uninstaller) + optional manual update
  check against GitHub Releases — updates never required; offline keeps working.
- **E:** Tauri shell **only if** native capabilities (tray, dialogs) become necessary.
